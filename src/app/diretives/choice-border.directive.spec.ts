import { ChoiceBorderDirective } from './choice-border.directive';

describe('ChoiceBorderDirective', () => {
  it('should create an instance', () => {
    const directive = new ChoiceBorderDirective();
    expect(directive).toBeTruthy();
  });
});
